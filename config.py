# Ajustes do sistema (mude aqui se precisar)
THRESHOLD = 0.38  # limiar de similaridade (cosine). >= dá acesso
DET_SIZE = (640, 640)  # resolução do detector
MODEL_NAME = "buffalo_l"  # pipeline padrão do InsightFace (detecção + reconhecimento)
DB_PATH = "face_access.db"
